export const BOARD_ROWS = 64
export const BOARD_COLS = BOARD_ROWS

export const APP_HEIGHT = window.innerHeight
export const APP_WIDTH = APP_HEIGHT

export const CELL_WIDTH = APP_WIDTH / BOARD_COLS
export const CELL_HEIGHT = APP_HEIGHT / BOARD_ROWS
